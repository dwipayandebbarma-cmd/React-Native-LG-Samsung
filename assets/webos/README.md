Put your webOS packaging assets here:

- `icon.png` (app icon)
- `splash.png` (optional splash image)

`npm run lg` copies these into `dist/` before creating the `.ipk`.
If you don't provide them, the build will generate tiny placeholder PNGs so packaging still works.

